spring:
  application:
    name: eurekaserver

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    server:
      eviction-interval-timer-in-ms: 30000 # Intervalo de evicção de 30 segundos
logging:
  level:
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF

